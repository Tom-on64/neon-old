(* Current Neon Syntax *)

program = statement*;

statement = (return|declaration);

declaration = type identifier "=" expression;
return = "return" expr;

expression = (intLit|identifier);

identifier = [A-Za-z_][A-Za-z0-9_]*;
type = ?type?;
intLit = ?LInt?;
